[tool.uv.workspace]
members = [
    "packages/marcus",
    "packages/moleman",
    "packages/morpheus",
    "packages/shared",
    "packages/sdk/python",
]

[project]
name = "makra-workspace"
version = "0.1.0"
requires-python = ">=3.12"